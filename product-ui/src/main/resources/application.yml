spring:
  application:
    name: product-ui

server:
  port: 8081

management:
  security:
    enabled: false
  endpoints:
    web:
      base-path: /
      exposure.include: "*"
  endpoint:
    health:
      show-details: always